{
  "name": "dirtyreefcleaning",
  "version": "1.0.0",
  "private": true,
  "description": "Reef Cleaning — Lincoln & Kearney (mirrored from production build)",
  "scripts": {
    "dev": "serve dist -l 5173",
    "dev:netlify": "netlify dev",
    "start": "npm run dev",
    "build": "node scripts/generate-config.js && node scripts/copy-form-bridge.js && node scripts/copy-perf.js && node scripts/patch-forms.js && node scripts/patch-css-fonts.js && node scripts/optimize-images.js && node seo/generate-seo.js",
    "sync:live": "node download-site.js",
    "seo": "node seo/generate-seo.js"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "sharp": "^0.34.5"
  }
}
